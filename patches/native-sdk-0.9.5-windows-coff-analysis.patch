diff --git a/build/app.zig b/build/app.zig
index 8216a52..5566918 100644
--- a/build/app.zig
+++ b/build/app.zig
@@ -1886,9 +1886,11 @@ pub fn addAppArtifacts(b: *std.Build, dep: *std.Build.Dependency, app_options: A
     // not just its test failures. Test builds never analyze `main` (the
     // test runner replaces the entry point), so rules that fire inside it
     // — UiApp.create's Model-defaults rule above all — used to ambush at
-    // `native build`, the LAST step in the loop. Compiling this object
-    // forces full semantic analysis of the app module, entry point
-    // included; nothing links or runs.
+    // `native build`, the LAST step in the loop. This artifact forces full
+    // semantic analysis of the app module, entry point included. It is never
+    // installed or run: non-Windows emits an object, while Windows links an
+    // uninstalled executable because COFF cannot partial-link the app's
+    // existing object and archive inputs.
     const analysis_root = b.addWriteFiles().add("app_analysis.zig",
         \\//! Generated by the app build: force semantic analysis of the
         \\//! app's entry point at test time. Exactly `main`, transitively —
@@ -1898,6 +1900,8 @@ pub fn addAppArtifacts(b: *std.Build, dep: *std.Build.Dependency, app_options: A
         \\    const app = @import("app");
         \\    if (@hasDecl(app, "main")) _ = &app.main;
         \\}
+        \\pub fn main() void {
+        \\}
         \\
     );
     const analysis_mod = b.createModule(.{
@@ -1906,9 +1910,13 @@ pub fn addAppArtifacts(b: *std.Build, dep: *std.Build.Dependency, app_options: A
         .optimize = optimize,
     });
     analysis_mod.addImport("app", test_app_mod);
-    const analysis_obj = b.addObject(.{
+    const analysis_obj = if (target.result.os.tag == .windows) b.addExecutable(.{
+        .name = b.fmt("{s}-analysis", .{app_options.name}),
+        .root_module = analysis_mod,
+        .use_llvm = useLlvmWorkaround(target),
+    }) else b.addObject(.{
         .name = b.fmt("{s}-analysis", .{app_options.name}),
         .root_module = analysis_mod,
         .use_llvm = useLlvmWorkaround(target),
     });
     test_step.dependOn(&analysis_obj.step);
